/** Borda padrão via token do tema. */
export const settingsBorderClass = 'border-border';

/** Painel suave (ex.: intervalos de horário, upload de voz). */
export const settingsMutedPanelClass = `rounded-lg border bg-surface-muted ${settingsBorderClass}`;

/** Cantos dos botões em Configurações. */
export const settingsControlClass = '!rounded-lg';

export const settingsSectionTitleClass = 'text-xl font-semibold tracking-tight text-foreground';

export const settingsSectionDescClass = 'mt-1 text-sm leading-relaxed text-foreground-muted';

export const settingsRowLabelClass = 'text-sm font-medium text-foreground';

export const settingsRowDescClass = 'text-sm leading-relaxed text-foreground-muted';

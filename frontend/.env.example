# O Vite está configurado com envDir na raiz do monorepo.
# Define VITE_* no ficheiro /.env na raiz (não crees frontend/.env).

# URL base da API (sem barra no fim). Produção Prestei:
VITE_API_URL=http://api.prestei.com
# Desenvolvimento local: VITE_API_URL=http://localhost:3001
